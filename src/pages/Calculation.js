import React from 'react';
import Calculator from '../components/Calculator';

const Calculation = () => (
  <div>
    <h2>
      Let&apos;s do some math!
    </h2>
    <Calculator />
  </div>
);

export default Calculation;
